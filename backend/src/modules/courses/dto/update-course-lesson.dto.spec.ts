import { UpdateCourseLessonDto } from './update-course-lesson.dto';

describe('UpdateCourseLessonDto', () => {
  it('should be defined', () => {
    expect(new UpdateCourseLessonDto()).toBeDefined();
  });
});
